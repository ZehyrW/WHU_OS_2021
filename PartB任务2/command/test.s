#include "type.h"
#include "stdio.h"

int main(int argc, char * argv[])
{
    const char * const hack[] = {"Ops!\n", "You have been hacked!\n"};
    printf("Hello, os!\n");
    int a = 2;
    int b = 3;
    printf("2 + 3 = %d\n", a + b);
    return 0;
}
